---
title: 記錄和攔截資料庫作業-EF6
description: 記錄和攔截 Entity Framework 6 中的資料庫作業
author: divega
ms.date: 10/23/2016
ms.assetid: b5ee7eb1-88cc-456e-b53c-c67e24c3f8ca
uid: ef6/fundamentals/logging-and-interception
ms.openlocfilehash: bb5c3392b4f2e1f291d7ac373d07724f56d0eb30
ms.sourcegitcommit: 7c3939504bb9da3f46bea3443638b808c04227c2
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 09/09/2020
ms.locfileid: "89616196"
---
# <a name="logging-and-intercepting-database-operations"></a><span data-ttu-id="38ac2-103">記錄和攔截資料庫作業</span><span class="sxs-lookup"><span data-stu-id="38ac2-103">Logging and intercepting database operations</span></span>
> [!NOTE]
> <span data-ttu-id="38ac2-104">**僅限 EF6 及更新版本** - Entity Framework 6 已引進此頁面中所討論的功能及 API 等等。</span><span class="sxs-lookup"><span data-stu-id="38ac2-104">**EF6 Onwards Only** - The features, APIs, etc. discussed in this page were introduced in Entity Framework 6.</span></span> <span data-ttu-id="38ac2-105">如果您使用的是較早版本，則不適用部分或全部的資訊。</span><span class="sxs-lookup"><span data-stu-id="38ac2-105">If you are using an earlier version, some or all of the information does not apply.</span></span>  

<span data-ttu-id="38ac2-106">從 Entity Framework 6 開始，每次 Entity Framework 將命令傳送至資料庫時，應用程式程式碼可以攔截此命令。</span><span class="sxs-lookup"><span data-stu-id="38ac2-106">Starting with Entity Framework 6, anytime Entity Framework sends a command to the database this command can be intercepted by application code.</span></span> <span data-ttu-id="38ac2-107">這最常用於記錄 SQL，但是也可以用來修改或中止命令。</span><span class="sxs-lookup"><span data-stu-id="38ac2-107">This is most commonly used for logging SQL, but can also be used to modify or abort the command.</span></span>  

<span data-ttu-id="38ac2-108">具體而言，EF 包含：</span><span class="sxs-lookup"><span data-stu-id="38ac2-108">Specifically, EF includes:</span></span>  

- <span data-ttu-id="38ac2-109">類似于 DataCoNtext 的內容記錄檔屬性 LINQ to SQL 中的記錄檔屬性</span><span class="sxs-lookup"><span data-stu-id="38ac2-109">A Log property for the context similar to DataContext.Log in LINQ to SQL</span></span>  
- <span data-ttu-id="38ac2-110">一種機制，可自訂傳送至記錄檔之輸出的內容和格式</span><span class="sxs-lookup"><span data-stu-id="38ac2-110">A mechanism to customize the content and formatting of the output sent to the log</span></span>  
- <span data-ttu-id="38ac2-111">適用于攔截的低層級建立區塊，提供更大的控制/彈性</span><span class="sxs-lookup"><span data-stu-id="38ac2-111">Low-level building blocks for interception giving greater control/flexibility</span></span>  

## <a name="context-log-property"></a><span data-ttu-id="38ac2-112">內容記錄檔屬性</span><span class="sxs-lookup"><span data-stu-id="38ac2-112">Context Log property</span></span>  

<span data-ttu-id="38ac2-113">您可以針對任何採用字串的方法，將 DbCoNtext 屬性設定為委派。</span><span class="sxs-lookup"><span data-stu-id="38ac2-113">The DbContext.Database.Log property can be set to a delegate for any method that takes a string.</span></span> <span data-ttu-id="38ac2-114">最常見的方法是將它設定為該該配置器的「寫入」方法，以搭配任何的任何值來使用。</span><span class="sxs-lookup"><span data-stu-id="38ac2-114">Most commonly it is used with any TextWriter by setting it to the “Write” method of that TextWriter.</span></span> <span data-ttu-id="38ac2-115">目前內容所產生的所有 SQL 都會記錄到該寫入器中。</span><span class="sxs-lookup"><span data-stu-id="38ac2-115">All SQL generated by the current context will be logged to that writer.</span></span> <span data-ttu-id="38ac2-116">例如，下列程式碼會將 SQL 記錄到主控台：</span><span class="sxs-lookup"><span data-stu-id="38ac2-116">For example, the following code will log SQL to the console:</span></span>  

``` csharp
using (var context = new BlogContext())
{
    context.Database.Log = Console.Write;

    // Your code here...
}
```  

<span data-ttu-id="38ac2-117">請注意該內容。資料庫 .Log 設定為 Console. Write。</span><span class="sxs-lookup"><span data-stu-id="38ac2-117">Notice that context.Database.Log is set to Console.Write.</span></span> <span data-ttu-id="38ac2-118">這是將 SQL 記錄到主控台所需的一切。</span><span class="sxs-lookup"><span data-stu-id="38ac2-118">This is all that is needed to log SQL to the console.</span></span>  

<span data-ttu-id="38ac2-119">讓我們新增一些簡單的查詢/插入/更新程式碼，讓我們可以看到一些輸出：</span><span class="sxs-lookup"><span data-stu-id="38ac2-119">Let’s add some simple query/insert/update code so that we can see some output:</span></span>  

``` csharp
using (var context = new BlogContext())
{
    context.Database.Log = Console.Write;

    var blog = context.Blogs.First(b => b.Title == "One Unicorn");

    blog.Posts.First().Title = "Green Eggs and Ham";

    blog.Posts.Add(new Post { Title = "I do not like them!" });

    context.SaveChangesAsync().Wait();
}
```  

<span data-ttu-id="38ac2-120">這會產生下列輸出：</span><span class="sxs-lookup"><span data-stu-id="38ac2-120">This will generate the following output:</span></span>  

``` SQL
SELECT TOP (1)
    [Extent1].[Id] AS [Id],
    [Extent1].[Title] AS [Title]
    FROM [dbo].[Blogs] AS [Extent1]
    WHERE (N'One Unicorn' = [Extent1].[Title]) AND ([Extent1].[Title] IS NOT NULL)
-- Executing at 10/8/2013 10:55:41 AM -07:00
-- Completed in 4 ms with result: SqlDataReader

SELECT
    [Extent1].[Id] AS [Id],
    [Extent1].[Title] AS [Title],
    [Extent1].[BlogId] AS [BlogId]
    FROM [dbo].[Posts] AS [Extent1]
    WHERE [Extent1].[BlogId] = @EntityKeyValue1
-- EntityKeyValue1: '1' (Type = Int32)
-- Executing at 10/8/2013 10:55:41 AM -07:00
-- Completed in 2 ms with result: SqlDataReader

UPDATE [dbo].[Posts]
SET [Title] = @0
WHERE ([Id] = @1)
-- @0: 'Green Eggs and Ham' (Type = String, Size = -1)
-- @1: '1' (Type = Int32)
-- Executing asynchronously at 10/8/2013 10:55:41 AM -07:00
-- Completed in 12 ms with result: 1

INSERT [dbo].[Posts]([Title], [BlogId])
VALUES (@0, @1)
SELECT [Id]
FROM [dbo].[Posts]
WHERE @@ROWCOUNT > 0 AND [Id] = scope_identity()
-- @0: 'I do not like them!' (Type = String, Size = -1)
-- @1: '1' (Type = Int32)
-- Executing asynchronously at 10/8/2013 10:55:41 AM -07:00
-- Completed in 2 ms with result: SqlDataReader
```  

<span data-ttu-id="38ac2-121"> (請注意，這是假設已發生任何資料庫初始化的輸出。</span><span class="sxs-lookup"><span data-stu-id="38ac2-121">(Note that this is the output assuming any database initialization has already happened.</span></span> <span data-ttu-id="38ac2-122">如果尚未進行資料庫初始化，則會有更多輸出顯示所有的工作遷移都在檢查或建立新的資料庫。 ) </span><span class="sxs-lookup"><span data-stu-id="38ac2-122">If database initialization had not already happened then there would be a lot more output showing all the work Migrations does under the covers to check for or create a new database.)</span></span>  

## <a name="what-gets-logged"></a><span data-ttu-id="38ac2-123">會記錄哪些專案？</span><span class="sxs-lookup"><span data-stu-id="38ac2-123">What gets logged?</span></span>  

<span data-ttu-id="38ac2-124">當記錄屬性設定完成時，將會記錄下列各項：</span><span class="sxs-lookup"><span data-stu-id="38ac2-124">When the Log property is set all of the following will be logged:</span></span>  

- <span data-ttu-id="38ac2-125">適用于所有不同類型命令的 SQL。</span><span class="sxs-lookup"><span data-stu-id="38ac2-125">SQL for all different kinds of commands.</span></span> <span data-ttu-id="38ac2-126">例如：</span><span class="sxs-lookup"><span data-stu-id="38ac2-126">For example:</span></span>  
    - <span data-ttu-id="38ac2-127">查詢，包括一般 LINQ 查詢、eSQL 查詢，以及來自方法（例如 SqlQuery）的原始查詢</span><span class="sxs-lookup"><span data-stu-id="38ac2-127">Queries, including normal LINQ queries, eSQL queries, and raw queries from methods such as SqlQuery</span></span>  
    - <span data-ttu-id="38ac2-128">作為 SaveChanges 一部分產生的插入、更新和刪除</span><span class="sxs-lookup"><span data-stu-id="38ac2-128">Inserts, updates, and deletes generated as part of SaveChanges</span></span>  
    - <span data-ttu-id="38ac2-129">關聯性載入查詢，例如消極式載入所產生的查詢</span><span class="sxs-lookup"><span data-stu-id="38ac2-129">Relationship loading queries such as those generated by lazy loading</span></span>  
- <span data-ttu-id="38ac2-130">參數</span><span class="sxs-lookup"><span data-stu-id="38ac2-130">Parameters</span></span>  
- <span data-ttu-id="38ac2-131">命令是否以非同步方式執行</span><span class="sxs-lookup"><span data-stu-id="38ac2-131">Whether or not the command is being executed asynchronously</span></span>  
- <span data-ttu-id="38ac2-132">指出命令開始執行的時間戳記</span><span class="sxs-lookup"><span data-stu-id="38ac2-132">A timestamp indicating when the command started executing</span></span>  
- <span data-ttu-id="38ac2-133">命令是否已順利完成、擲回例外狀況失敗，或已取消（非同步）</span><span class="sxs-lookup"><span data-stu-id="38ac2-133">Whether or not the command completed successfully, failed by throwing an exception, or, for async, was canceled</span></span>  
- <span data-ttu-id="38ac2-134">結果值的某些指示</span><span class="sxs-lookup"><span data-stu-id="38ac2-134">Some indication of the result value</span></span>  
- <span data-ttu-id="38ac2-135">執行命令所花費的大約時間量。</span><span class="sxs-lookup"><span data-stu-id="38ac2-135">The approximate amount of time it took to execute the command.</span></span> <span data-ttu-id="38ac2-136">請注意，這是傳送命令以取回結果物件的時間。</span><span class="sxs-lookup"><span data-stu-id="38ac2-136">Note that this is the time from sending the command to getting the result object back.</span></span> <span data-ttu-id="38ac2-137">它不包含讀取結果的時間。</span><span class="sxs-lookup"><span data-stu-id="38ac2-137">It does not include time to read the results.</span></span>  

<span data-ttu-id="38ac2-138">查看上述的範例輸出，每四個命令都會記錄：</span><span class="sxs-lookup"><span data-stu-id="38ac2-138">Looking at the example output above, each of the four commands logged are:</span></span>  

- <span data-ttu-id="38ac2-139">對內容的呼叫所產生的查詢。Blog。 First</span><span class="sxs-lookup"><span data-stu-id="38ac2-139">The query resulting from the call to context.Blogs.First</span></span>  
    - <span data-ttu-id="38ac2-140">請注意，取得 SQL 的 ToString 方法對此查詢沒有作用，因為 "First" 並未提供可呼叫 ToString 的 IQueryable。</span><span class="sxs-lookup"><span data-stu-id="38ac2-140">Notice that the ToString method of getting the SQL would not have worked for this query since “First” does not provide an IQueryable on which ToString could be called</span></span>  
- <span data-ttu-id="38ac2-141">從延遲載入的 blog 產生的查詢。職位</span><span class="sxs-lookup"><span data-stu-id="38ac2-141">The query resulting from the lazy-loading of blog.Posts</span></span>  
    - <span data-ttu-id="38ac2-142">請注意發生消極式載入的金鑰值的參數詳細資料</span><span class="sxs-lookup"><span data-stu-id="38ac2-142">Notice the parameter details for the key value for which lazy loading is happening</span></span>  
    - <span data-ttu-id="38ac2-143">只會記錄設定為非預設值之參數的屬性。</span><span class="sxs-lookup"><span data-stu-id="38ac2-143">Only properties of the parameter that are set to non-default values are logged.</span></span> <span data-ttu-id="38ac2-144">例如，Size 屬性只有在非零的情況下才會顯示。</span><span class="sxs-lookup"><span data-stu-id="38ac2-144">For example, the Size property is only shown if it is non-zero.</span></span>  
- <span data-ttu-id="38ac2-145">SaveChangesAsync 所產生的兩個命令;一個用於變更貼文標題的更新，另一個則用於插入以新增貼文</span><span class="sxs-lookup"><span data-stu-id="38ac2-145">Two commands resulting from SaveChangesAsync; one for the update to change a post title, the other for an insert to add a new post</span></span>  
    - <span data-ttu-id="38ac2-146">請注意 FK 和 Title 屬性的參數詳細資料</span><span class="sxs-lookup"><span data-stu-id="38ac2-146">Notice the parameter details for the FK and Title properties</span></span>  
    - <span data-ttu-id="38ac2-147">請注意，這些命令是以非同步方式執行</span><span class="sxs-lookup"><span data-stu-id="38ac2-147">Notice that these commands are being executed asynchronously</span></span>  

## <a name="logging-to-different-places"></a><span data-ttu-id="38ac2-148">記錄到不同的位置</span><span class="sxs-lookup"><span data-stu-id="38ac2-148">Logging to different places</span></span>  

<span data-ttu-id="38ac2-149">如上所示，您可以輕鬆地登入主控台。</span><span class="sxs-lookup"><span data-stu-id="38ac2-149">As shown above logging to the console is super easy.</span></span> <span data-ttu-id="38ac2-150">您也可以使用不同 [種類的配置](https://msdn.microsoft.com/library/system.io.textwriter.aspx)檔，輕鬆地記錄至記憶體、檔案等。</span><span class="sxs-lookup"><span data-stu-id="38ac2-150">It’s also easy to log to memory, file, etc. by using different kinds of [TextWriter](https://msdn.microsoft.com/library/system.io.textwriter.aspx).</span></span>  

<span data-ttu-id="38ac2-151">如果您很熟悉 LINQ to SQL 您可能會注意到，LINQ to SQL 記錄檔屬性已設定為實際的預設值物件 (例如，主控台) 。在 EF 中，Log 屬性會設定為可接受字串的方法，例如，Console. Write 或 Console. Write (。</span><span class="sxs-lookup"><span data-stu-id="38ac2-151">If you are familiar with LINQ to SQL you might notice that in LINQ to SQL the Log property is set to the actual TextWriter object (for example, Console.Out) while in EF the Log property is set to a method that accepts a string (for example, Console.Write or Console.Out.Write).</span></span> <span data-ttu-id="38ac2-152">這種情況的原因是要藉由接受可作為字串接收的任何委派，來將 EF 與確定分離。</span><span class="sxs-lookup"><span data-stu-id="38ac2-152">The reason for this is to decouple EF from TextWriter by accepting any delegate that can act as a sink for strings.</span></span> <span data-ttu-id="38ac2-153">例如，假設您已經有一些記錄架構，它會定義如下的記錄方法：</span><span class="sxs-lookup"><span data-stu-id="38ac2-153">For example, imagine that you already have some logging framework and it defines a logging method like so:</span></span>  

``` csharp
public class MyLogger
{
    public void Log(string component, string message)
    {
        Console.WriteLine("Component: {0} Message: {1} ", component, message);
    }
}
```  

<span data-ttu-id="38ac2-154">這可能會連結至 EF 記錄檔屬性，如下所示：</span><span class="sxs-lookup"><span data-stu-id="38ac2-154">This could be hooked up to the EF Log property like this:</span></span>  

``` csharp
var logger = new MyLogger();
context.Database.Log = s => logger.Log("EFApp", s);
```  

## <a name="result-logging"></a><span data-ttu-id="38ac2-155">結果記錄</span><span class="sxs-lookup"><span data-stu-id="38ac2-155">Result logging</span></span>  

<span data-ttu-id="38ac2-156">預設記錄器會記錄命令文字 (SQL) 、參數，以及將命令傳送至資料庫之前，具有時間戳記的「執行中」行。</span><span class="sxs-lookup"><span data-stu-id="38ac2-156">The default logger logs command text (SQL), parameters, and the “Executing” line with a timestamp before the command is sent to the database.</span></span> <span data-ttu-id="38ac2-157">執行命令後，會記錄包含經過時間的「已完成」行。</span><span class="sxs-lookup"><span data-stu-id="38ac2-157">A “completed” line containing elapsed time is logged following execution of the command.</span></span>  

<span data-ttu-id="38ac2-158">請注意，針對非同步命令，在非同步工作實際完成、失敗或取消之前，不會記錄「已完成」行。</span><span class="sxs-lookup"><span data-stu-id="38ac2-158">Note that for async commands the “completed” line is not logged until the async task actually completes, fails, or is canceled.</span></span>  

<span data-ttu-id="38ac2-159">「已完成」行包含不同的資訊，這取決於命令類型以及是否成功執行。</span><span class="sxs-lookup"><span data-stu-id="38ac2-159">The “completed” line contains different information depending on the type of command and whether or not execution was successful.</span></span>  

### <a name="successful-execution"></a><span data-ttu-id="38ac2-160">成功執行</span><span class="sxs-lookup"><span data-stu-id="38ac2-160">Successful execution</span></span>  

<span data-ttu-id="38ac2-161">若為順利完成的命令，輸出會是「在 x 毫秒內完成，並產生結果：」，後面接著某個結果的指示。</span><span class="sxs-lookup"><span data-stu-id="38ac2-161">For commands that complete successfully the output is “Completed in x ms with result: “ followed by some indication of what the result was.</span></span> <span data-ttu-id="38ac2-162">針對傳回資料讀取器的命令，結果指示是傳回的 [DbDataReader](https://msdn.microsoft.com/library/system.data.common.dbdatareader.aspx) 類型。</span><span class="sxs-lookup"><span data-stu-id="38ac2-162">For commands that return a data reader the result indication is the type of [DbDataReader](https://msdn.microsoft.com/library/system.data.common.dbdatareader.aspx) returned.</span></span> <span data-ttu-id="38ac2-163">針對傳回整數值的命令，例如上面所示的 update 命令，結果就是該整數。</span><span class="sxs-lookup"><span data-stu-id="38ac2-163">For commands that return an integer value such as the update command shown above the result shown is that integer.</span></span>  

### <a name="failed-execution"></a><span data-ttu-id="38ac2-164">失敗的執行</span><span class="sxs-lookup"><span data-stu-id="38ac2-164">Failed execution</span></span>  

<span data-ttu-id="38ac2-165">針對因擲回例外狀況而失敗的命令，輸出會包含來自例外狀況的訊息。</span><span class="sxs-lookup"><span data-stu-id="38ac2-165">For commands that fail by throwing an exception, the output contains the message from the exception.</span></span> <span data-ttu-id="38ac2-166">例如，使用 SqlQuery 針對存在的資料表進行查詢，將會導致記錄輸出，如下所示：</span><span class="sxs-lookup"><span data-stu-id="38ac2-166">For example, using SqlQuery to query against a table that does exist will result in log output something like this:</span></span>  

``` SQL
SELECT * from ThisTableIsMissing
-- Executing at 5/13/2013 10:19:05 AM
-- Failed in 1 ms with error: Invalid object name 'ThisTableIsMissing'.
```  

### <a name="canceled-execution"></a><span data-ttu-id="38ac2-167">已取消執行</span><span class="sxs-lookup"><span data-stu-id="38ac2-167">Canceled execution</span></span>  

<span data-ttu-id="38ac2-168">對於取消工作的非同步命令，結果可能會因例外狀況而失敗，因為這是基礎 ADO.NET 提供者在嘗試取消時通常會執行的作業。</span><span class="sxs-lookup"><span data-stu-id="38ac2-168">For async commands where the task is canceled the result could be failure with an exception, since this is what the underlying ADO.NET provider often does when an attempt is made to cancel.</span></span> <span data-ttu-id="38ac2-169">如果這不會發生，而且工作已完全取消，則輸出看起來會像這樣：</span><span class="sxs-lookup"><span data-stu-id="38ac2-169">If this doesn’t happen and the task is canceled cleanly then the output will look something like this:</span></span>  

```console
update Blogs set Title = 'No' where Id = -1
-- Executing asynchronously at 5/13/2013 10:21:10 AM
-- Canceled in 1 ms
```  

## <a name="changing-log-content-and-formatting"></a><span data-ttu-id="38ac2-170">變更記錄內容和格式</span><span class="sxs-lookup"><span data-stu-id="38ac2-170">Changing log content and formatting</span></span>  

<span data-ttu-id="38ac2-171">在中，Database .Log 屬性會使用 DatabaseLogFormatter 物件。</span><span class="sxs-lookup"><span data-stu-id="38ac2-171">Under the covers the Database.Log property makes use of a DatabaseLogFormatter object.</span></span> <span data-ttu-id="38ac2-172">這個物件會有效地系結 IDbCommandInterceptor 的實 (請參閱以下) 到接受字串和 DbCoNtext 的委派。</span><span class="sxs-lookup"><span data-stu-id="38ac2-172">This object effectively binds an IDbCommandInterceptor implementation (see below) to a delegate that accepts strings and a DbContext.</span></span> <span data-ttu-id="38ac2-173">這表示 DatabaseLogFormatter 上的方法會在 EF 執行命令之前和之後呼叫。</span><span class="sxs-lookup"><span data-stu-id="38ac2-173">This means that methods on DatabaseLogFormatter are called before and after the execution of commands by EF.</span></span> <span data-ttu-id="38ac2-174">這些 DatabaseLogFormatter 方法會收集並格式化記錄輸出，並將其傳送給委派。</span><span class="sxs-lookup"><span data-stu-id="38ac2-174">These DatabaseLogFormatter methods gather and format log output and send it to the delegate.</span></span>  

### <a name="customizing-databaselogformatter"></a><span data-ttu-id="38ac2-175">自訂 DatabaseLogFormatter</span><span class="sxs-lookup"><span data-stu-id="38ac2-175">Customizing DatabaseLogFormatter</span></span>  

<span data-ttu-id="38ac2-176">您可以建立衍生自 DatabaseLogFormatter 的新類別，並視需要覆寫方法，以變更所記錄的內容和格式。</span><span class="sxs-lookup"><span data-stu-id="38ac2-176">Changing what is logged and how it is formatted can be achieved by creating a new class that derives from DatabaseLogFormatter and overrides methods as appropriate.</span></span> <span data-ttu-id="38ac2-177">最常見的覆寫方法如下：</span><span class="sxs-lookup"><span data-stu-id="38ac2-177">The most common methods to override are:</span></span>  

- <span data-ttu-id="38ac2-178">LogCommand –覆寫此參數以變更命令在執行之前的記錄方式。</span><span class="sxs-lookup"><span data-stu-id="38ac2-178">LogCommand – Override this to change how commands are logged before they are executed.</span></span> <span data-ttu-id="38ac2-179">依預設，LogCommand 會呼叫每個參數的 LogParameter;您可以選擇在覆寫中進行相同的動作，或改以不同的方式處理參數。</span><span class="sxs-lookup"><span data-stu-id="38ac2-179">By default LogCommand calls LogParameter for each parameter; you may choose to do the same in your override or handle parameters differently instead.</span></span>  
- <span data-ttu-id="38ac2-180">LogResult –覆寫此項，以變更如何記錄執行命令的結果。</span><span class="sxs-lookup"><span data-stu-id="38ac2-180">LogResult – Override this to change how the outcome from executing a command is logged.</span></span>  
- <span data-ttu-id="38ac2-181">LogParameter –覆寫此參數以變更參數記錄的格式和內容。</span><span class="sxs-lookup"><span data-stu-id="38ac2-181">LogParameter – Override this to change the formatting and content of parameter logging.</span></span>  

<span data-ttu-id="38ac2-182">例如，假設我們想要在每個命令傳送至資料庫之前，只記錄一行。</span><span class="sxs-lookup"><span data-stu-id="38ac2-182">For example, suppose we wanted to log just a single line before each command is sent to the database.</span></span> <span data-ttu-id="38ac2-183">這可以透過兩個覆寫來完成：</span><span class="sxs-lookup"><span data-stu-id="38ac2-183">This can be done with two overrides:</span></span>  

- <span data-ttu-id="38ac2-184">覆寫 LogCommand 以格式化及撰寫單一的 SQL 行</span><span class="sxs-lookup"><span data-stu-id="38ac2-184">Override LogCommand to format and write the single line of SQL</span></span>  
- <span data-ttu-id="38ac2-185">覆寫 LogResult 不執行任何動作。</span><span class="sxs-lookup"><span data-stu-id="38ac2-185">Override LogResult to do nothing.</span></span>  

<span data-ttu-id="38ac2-186">程式碼應該類似如下所示：</span><span class="sxs-lookup"><span data-stu-id="38ac2-186">The code would look something like this:</span></span>

``` csharp
public class OneLineFormatter : DatabaseLogFormatter
{
    public OneLineFormatter(DbContext context, Action<string> writeAction)
        : base(context, writeAction)
    {
    }

    public override void LogCommand<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
        Write(string.Format(
            "Context '{0}' is executing command '{1}'{2}",
            Context.GetType().Name,
            command.CommandText.Replace(Environment.NewLine, ""),
            Environment.NewLine));
    }

    public override void LogResult<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
    }
}
```  

<span data-ttu-id="38ac2-187">若要記錄輸出，只要呼叫寫入方法，將輸出傳送至設定的寫入委派即可。</span><span class="sxs-lookup"><span data-stu-id="38ac2-187">To log output simply call the Write method which will send output to the configured write delegate.</span></span>  

<span data-ttu-id="38ac2-188"> (請注意，此程式碼會簡化分行符號的移除，就像範例一樣。</span><span class="sxs-lookup"><span data-stu-id="38ac2-188">(Note that this code does simplistic removal of line breaks just as an example.</span></span> <span data-ttu-id="38ac2-189">它可能不適用於觀看複雜的 SQL。 ) </span><span class="sxs-lookup"><span data-stu-id="38ac2-189">It will likely not work well for viewing complex SQL.)</span></span>  

### <a name="setting-the-databaselogformatter"></a><span data-ttu-id="38ac2-190">設定 DatabaseLogFormatter</span><span class="sxs-lookup"><span data-stu-id="38ac2-190">Setting the DatabaseLogFormatter</span></span>  

<span data-ttu-id="38ac2-191">建立新的 DatabaseLogFormatter 類別之後，必須向 EF 註冊。</span><span class="sxs-lookup"><span data-stu-id="38ac2-191">Once a new DatabaseLogFormatter class has been created it needs to be registered with EF.</span></span> <span data-ttu-id="38ac2-192">這是使用以程式碼為基礎的設定來完成。</span><span class="sxs-lookup"><span data-stu-id="38ac2-192">This is done using code-based configuration.</span></span> <span data-ttu-id="38ac2-193">總而言之，這表示建立新的類別，該類別衍生自 >dbconfiguration 在相同元件中的 DbCoNtext 類別，然後在這個新類別的函式中呼叫 SetDatabaseLogFormatter。</span><span class="sxs-lookup"><span data-stu-id="38ac2-193">In a nutshell this means creating a new class that derives from DbConfiguration in the same assembly as your DbContext class and then calling SetDatabaseLogFormatter in the constructor of this new class.</span></span> <span data-ttu-id="38ac2-194">例如：</span><span class="sxs-lookup"><span data-stu-id="38ac2-194">For example:</span></span>  

``` csharp
public class MyDbConfiguration : DbConfiguration
{
    public MyDbConfiguration()
    {
        SetDatabaseLogFormatter(
            (context, writeAction) => new OneLineFormatter(context, writeAction));
    }
}
```  

### <a name="using-the-new-databaselogformatter"></a><span data-ttu-id="38ac2-195">使用新的 DatabaseLogFormatter</span><span class="sxs-lookup"><span data-stu-id="38ac2-195">Using the new DatabaseLogFormatter</span></span>  

<span data-ttu-id="38ac2-196">這個新的 DatabaseLogFormatter 現在會在每次設定資料庫時使用。</span><span class="sxs-lookup"><span data-stu-id="38ac2-196">This new DatabaseLogFormatter will now be used anytime Database.Log is set.</span></span> <span data-ttu-id="38ac2-197">因此，從第1部分執行程式碼現在會產生下列輸出：</span><span class="sxs-lookup"><span data-stu-id="38ac2-197">So, running the code from part 1 will now result in the following output:</span></span>  

```console
Context 'BlogContext' is executing command 'SELECT TOP (1) [Extent1].[Id] AS [Id], [Extent1].[Title] AS [Title]FROM [dbo].[Blogs] AS [Extent1]WHERE (N'One Unicorn' = [Extent1].[Title]) AND ([Extent1].[Title] IS NOT NULL)'
Context 'BlogContext' is executing command 'SELECT [Extent1].[Id] AS [Id], [Extent1].[Title] AS [Title], [Extent1].[BlogId] AS [BlogId]FROM [dbo].[Posts] AS [Extent1]WHERE [Extent1].[BlogId] = @EntityKeyValue1'
Context 'BlogContext' is executing command 'update [dbo].[Posts]set [Title] = @0where ([Id] = @1)'
Context 'BlogContext' is executing command 'insert [dbo].[Posts]([Title], [BlogId])values (@0, @1)select [Id]from [dbo].[Posts]where @@rowcount > 0 and [Id] = scope_identity()'
```  

## <a name="interception-building-blocks"></a><span data-ttu-id="38ac2-198">攔截建立區塊</span><span class="sxs-lookup"><span data-stu-id="38ac2-198">Interception building blocks</span></span>  

<span data-ttu-id="38ac2-199">到目前為止，我們已經討論過如何使用 DbCoNtext 來記錄 EF 所產生的 SQL。</span><span class="sxs-lookup"><span data-stu-id="38ac2-199">So far we have looked at how to use DbContext.Database.Log to log the SQL generated by EF.</span></span> <span data-ttu-id="38ac2-200">但在某些低層級的建立區塊中，此程式碼實際上是相當精簡的外觀，以進行一般攔截。</span><span class="sxs-lookup"><span data-stu-id="38ac2-200">But this code is actually a relatively thin facade over some low-level building blocks for more general interception.</span></span>  

### <a name="interception-interfaces"></a><span data-ttu-id="38ac2-201">攔截介面</span><span class="sxs-lookup"><span data-stu-id="38ac2-201">Interception interfaces</span></span>  

<span data-ttu-id="38ac2-202">攔截程式碼是圍繞攔截介面的概念所建立。</span><span class="sxs-lookup"><span data-stu-id="38ac2-202">The interception code is built around the concept of interception interfaces.</span></span> <span data-ttu-id="38ac2-203">這些介面繼承自 IDbInterceptor，並定義 EF 執行某些動作時所呼叫的方法。</span><span class="sxs-lookup"><span data-stu-id="38ac2-203">These interfaces inherit from IDbInterceptor and define methods that are called when EF performs some action.</span></span> <span data-ttu-id="38ac2-204">其目的是要攔截的每個物件類型都有一個介面。</span><span class="sxs-lookup"><span data-stu-id="38ac2-204">The intent is to have one interface per type of object being intercepted.</span></span> <span data-ttu-id="38ac2-205">例如，IDbCommandInterceptor 介面會定義在 EF 呼叫 ExecuteNonQuery、ExecuteScalar、ExecuteReader 和相關方法之前所呼叫的方法。</span><span class="sxs-lookup"><span data-stu-id="38ac2-205">For example, the IDbCommandInterceptor interface defines methods that are called before EF makes a call to ExecuteNonQuery, ExecuteScalar, ExecuteReader, and related methods.</span></span> <span data-ttu-id="38ac2-206">同樣地，介面會定義在每個作業完成時所呼叫的方法。</span><span class="sxs-lookup"><span data-stu-id="38ac2-206">Likewise, the interface defines methods that are called when each of these operations completes.</span></span> <span data-ttu-id="38ac2-207">我們在上面探討的 DatabaseLogFormatter 類別會執行此介面來記錄命令。</span><span class="sxs-lookup"><span data-stu-id="38ac2-207">The DatabaseLogFormatter class that we looked at above implements this interface to log commands.</span></span>  

### <a name="the-interception-context"></a><span data-ttu-id="38ac2-208">攔截內容</span><span class="sxs-lookup"><span data-stu-id="38ac2-208">The interception context</span></span>  

<span data-ttu-id="38ac2-209">查看任何攔截器介面上定義的方法，很明顯地，每個呼叫都有一個 DbInterceptionCoNtext 類型的物件，或是一些衍生自這個類別的型別，例如 DbCommandInterceptionCoNtext \<\> 。</span><span class="sxs-lookup"><span data-stu-id="38ac2-209">Looking at the methods defined on any of the interceptor interfaces it is apparent that every call is given an object of type DbInterceptionContext or some type derived from this such as DbCommandInterceptionContext\<\>.</span></span> <span data-ttu-id="38ac2-210">此物件包含 EF 所採取之動作的相關內容資訊。</span><span class="sxs-lookup"><span data-stu-id="38ac2-210">This object contains contextual information about the action that EF is taking.</span></span> <span data-ttu-id="38ac2-211">例如，如果正在代表 DbCoNtext 執行動作，則 DbCoNtext 會包含在 DbInterceptionCoNtext 中。</span><span class="sxs-lookup"><span data-stu-id="38ac2-211">For example, if the action is being taken on behalf of a DbContext, then the DbContext is included in the DbInterceptionContext.</span></span> <span data-ttu-id="38ac2-212">同樣地，對於要以非同步方式執行的命令，IsAsync 旗標是在 DbCommandInterceptionCoNtext 上設定的。</span><span class="sxs-lookup"><span data-stu-id="38ac2-212">Similarly, for commands that are being executed asynchronously, the IsAsync flag is set on DbCommandInterceptionContext.</span></span>  

### <a name="result-handling"></a><span data-ttu-id="38ac2-213">結果處理</span><span class="sxs-lookup"><span data-stu-id="38ac2-213">Result handling</span></span>  

<span data-ttu-id="38ac2-214">DbCommandInterceptionCoNtext \<\> 類別包含一個稱為 Result、OriginalResult、Exception 和 OriginalException 的屬性。</span><span class="sxs-lookup"><span data-stu-id="38ac2-214">The DbCommandInterceptionContext\<\> class contains a properties called Result, OriginalResult, Exception, and OriginalException.</span></span> <span data-ttu-id="38ac2-215">這些屬性會設定為 null/零，用於呼叫在執行作業之前呼叫的攔截方法，也就是 .。。正在執行方法。</span><span class="sxs-lookup"><span data-stu-id="38ac2-215">These properties are set to null/zero for calls to the interception methods that are called before the operation is executed — that is, for the …Executing methods.</span></span> <span data-ttu-id="38ac2-216">如果作業已執行且成功，則結果和 OriginalResult 會設定為作業的結果。</span><span class="sxs-lookup"><span data-stu-id="38ac2-216">If the operation is executed and succeeds, then Result and OriginalResult are set to the result of the operation.</span></span> <span data-ttu-id="38ac2-217">然後，您可以在作業執行後呼叫的攔截方法中觀察到這些值（也就是在 .。。執行的方法。</span><span class="sxs-lookup"><span data-stu-id="38ac2-217">These values can then be observed in the interception methods that are called after the operation has executed — that is, on the …Executed methods.</span></span> <span data-ttu-id="38ac2-218">同樣地，如果作業擲回，則會設定例外狀況和 OriginalException 屬性。</span><span class="sxs-lookup"><span data-stu-id="38ac2-218">Likewise, if the operation throws, then the Exception and OriginalException properties will be set.</span></span>  

#### <a name="suppressing-execution"></a><span data-ttu-id="38ac2-219">隱藏執行</span><span class="sxs-lookup"><span data-stu-id="38ac2-219">Suppressing execution</span></span>  

<span data-ttu-id="38ac2-220">如果攔截器設定結果屬性，然後命令已執行 (在其中一個 .。。執行方法) 接著 EF 不會嘗試實際執行命令，而只會使用結果集。</span><span class="sxs-lookup"><span data-stu-id="38ac2-220">If an interceptor sets the Result property before the command has executed (in one of the …Executing methods) then EF will not attempt to actually execute the command, but will instead just use the result set.</span></span> <span data-ttu-id="38ac2-221">換句話說，攔截器可以隱藏命令的執行，但讓 EF 繼續執行，就像命令已執行一樣。</span><span class="sxs-lookup"><span data-stu-id="38ac2-221">In other words, the interceptor can suppress execution of the command but have EF continue as if the command had been executed.</span></span>  

<span data-ttu-id="38ac2-222">使用這個方法的範例，是傳統上使用包裝提供者完成的命令批次處理。</span><span class="sxs-lookup"><span data-stu-id="38ac2-222">An example of how this might be used is the command batching that has traditionally been done with a wrapping provider.</span></span> <span data-ttu-id="38ac2-223">攔截器會以批次方式儲存命令以供稍後執行，但會將命令「假裝」為 EF，命令已正常執行。</span><span class="sxs-lookup"><span data-stu-id="38ac2-223">The interceptor would store the command for later execution as a batch but would “pretend” to EF that the command had executed as normal.</span></span> <span data-ttu-id="38ac2-224">請注意，它需要的不只是為了執行批次處理，但這是可能使用變更攔截結果的範例。</span><span class="sxs-lookup"><span data-stu-id="38ac2-224">Note that it requires more than this to implement batching, but this is an example of how changing the interception result might be used.</span></span>  

<span data-ttu-id="38ac2-225">您也可以藉由設定其中一個 .。。正在執行方法。</span><span class="sxs-lookup"><span data-stu-id="38ac2-225">Execution can also be suppressed by setting the Exception property in one of the …Executing methods.</span></span> <span data-ttu-id="38ac2-226">這會導致 EF 藉由擲回指定的例外狀況而繼續執行作業失敗。</span><span class="sxs-lookup"><span data-stu-id="38ac2-226">This causes EF to continue as if execution of the operation had failed by throwing the given exception.</span></span> <span data-ttu-id="38ac2-227">當然，這可能會導致應用程式損毀，但也可能是暫時性例外狀況，或是 EF 所處理的其他例外狀況。</span><span class="sxs-lookup"><span data-stu-id="38ac2-227">This may, of course, cause the application to crash, but it may also be a transient exception or some other exception that is handled by EF.</span></span> <span data-ttu-id="38ac2-228">例如，這可以在測試環境中用來測試命令執行失敗時的應用程式行為。</span><span class="sxs-lookup"><span data-stu-id="38ac2-228">For example, this could be used in test environments to test the behavior of an application when command execution fails.</span></span>  

#### <a name="changing-the-result-after-execution"></a><span data-ttu-id="38ac2-229">在執行之後變更結果</span><span class="sxs-lookup"><span data-stu-id="38ac2-229">Changing the result after execution</span></span>  

<span data-ttu-id="38ac2-230">如果攔截器在命令執行之後設定 Result 屬性 (在其中一個 .。。執行的方法) 接著 EF 會使用變更的結果，而不是實際從作業傳回的結果。</span><span class="sxs-lookup"><span data-stu-id="38ac2-230">If an interceptor sets the Result property after the command has executed (in one of the …Executed methods) then EF will use the changed result instead of the result that was actually returned from the operation.</span></span> <span data-ttu-id="38ac2-231">同樣地，如果攔截器在命令執行之後設定 Exception 屬性，EF 將擲回 set 例外狀況，就像作業擲回例外狀況一樣。</span><span class="sxs-lookup"><span data-stu-id="38ac2-231">Similarly, if an interceptor sets the Exception property after the command has executed, then EF will throw the set exception as if the operation had thrown the exception.</span></span>  

<span data-ttu-id="38ac2-232">攔截器也可以將 Exception 屬性設為 null，表示不應該擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="38ac2-232">An interceptor can also set the Exception property to null to indicate that no exception should be thrown.</span></span> <span data-ttu-id="38ac2-233">如果執行作業失敗，但攔截器希望 EF 繼續，就像作業成功一樣，這會很有用。</span><span class="sxs-lookup"><span data-stu-id="38ac2-233">This can be useful if execution of the operation failed but the interceptor wishes EF to continue as if the operation had succeeded.</span></span> <span data-ttu-id="38ac2-234">這通常也會牽涉到設定結果，讓 EF 有一些結果值在繼續時仍能運作。</span><span class="sxs-lookup"><span data-stu-id="38ac2-234">This usually also involves setting the Result so that EF has some result value to work with as it continues.</span></span>  

#### <a name="originalresult-and-originalexception"></a><span data-ttu-id="38ac2-235">OriginalResult 和 OriginalException</span><span class="sxs-lookup"><span data-stu-id="38ac2-235">OriginalResult and OriginalException</span></span>  

<span data-ttu-id="38ac2-236">在 EF 執行作業之後，它會在執行失敗時設定結果和 OriginalResult 屬性，如果執行失敗且發生例外狀況，則會設定例外狀況和 OriginalException 屬性。</span><span class="sxs-lookup"><span data-stu-id="38ac2-236">After EF has executed an operation it will set either the Result and OriginalResult properties if execution did not fail or the Exception and OriginalException properties if execution failed with an exception.</span></span>  

<span data-ttu-id="38ac2-237">OriginalResult 和 OriginalException 屬性是唯讀的，而且只會在實際執行作業之後由 EF 設定。</span><span class="sxs-lookup"><span data-stu-id="38ac2-237">The OriginalResult and OriginalException properties are read-only and are only set by EF after actually executing an operation.</span></span> <span data-ttu-id="38ac2-238">攔截器無法設定這些屬性。</span><span class="sxs-lookup"><span data-stu-id="38ac2-238">These properties cannot be set by interceptors.</span></span> <span data-ttu-id="38ac2-239">這表示任何攔截器都可以區分某些其他攔截器已設定的例外狀況或結果，而不是執行作業時所發生的實際例外狀況或結果。</span><span class="sxs-lookup"><span data-stu-id="38ac2-239">This means that any interceptor can distinguish between an exception or result that has been set by some other interceptor as opposed to the real exception or result that occurred when the operation was executed.</span></span>  

### <a name="registering-interceptors"></a><span data-ttu-id="38ac2-240">註冊攔截器</span><span class="sxs-lookup"><span data-stu-id="38ac2-240">Registering interceptors</span></span>  

<span data-ttu-id="38ac2-241">一旦建立了一個或多個攔截介面的類別之後，就可以使用 DbInterception 類別向 EF 註冊。</span><span class="sxs-lookup"><span data-stu-id="38ac2-241">Once a class that implements one or more of the interception interfaces has been created it can be registered with EF using the DbInterception class.</span></span> <span data-ttu-id="38ac2-242">例如：</span><span class="sxs-lookup"><span data-stu-id="38ac2-242">For example:</span></span>  

``` csharp
DbInterception.Add(new NLogCommandInterceptor());
```  

<span data-ttu-id="38ac2-243">您也可以使用 >dbconfiguration 程式碼式設定機制，在應用程式域層級註冊攔截器。</span><span class="sxs-lookup"><span data-stu-id="38ac2-243">Interceptors can also be registered at the app-domain level using the DbConfiguration code-based configuration mechanism.</span></span>  

### <a name="example-logging-to-nlog"></a><span data-ttu-id="38ac2-244">範例：記錄至 NLog</span><span class="sxs-lookup"><span data-stu-id="38ac2-244">Example: Logging to NLog</span></span>  

<span data-ttu-id="38ac2-245">讓我們將這些全部整合在一起的範例中，使用 IDbCommandInterceptor 和 [NLog](https://nlog-project.org/) ：</span><span class="sxs-lookup"><span data-stu-id="38ac2-245">Let’s put all this together into an example that using IDbCommandInterceptor and [NLog](https://nlog-project.org/) to:</span></span>  

- <span data-ttu-id="38ac2-246">針對任何以非非同步方式執行的命令記錄警告</span><span class="sxs-lookup"><span data-stu-id="38ac2-246">Log a warning for any command that is executed non-asynchronously</span></span>  
- <span data-ttu-id="38ac2-247">記錄執行時擲回之任何命令的錯誤</span><span class="sxs-lookup"><span data-stu-id="38ac2-247">Log an error for any command that throws when executed</span></span>  

<span data-ttu-id="38ac2-248">以下是執行記錄的類別，應該註冊如下所示：</span><span class="sxs-lookup"><span data-stu-id="38ac2-248">Here’s the class that does the logging, which should be registered as shown above:</span></span>  

``` csharp
public class NLogCommandInterceptor : IDbCommandInterceptor
{
    private static readonly Logger Logger = LogManager.GetCurrentClassLogger();

    public void NonQueryExecuting(
        DbCommand command, DbCommandInterceptionContext<int> interceptionContext)
    {
        LogIfNonAsync(command, interceptionContext);
    }

    public void NonQueryExecuted(
        DbCommand command, DbCommandInterceptionContext<int> interceptionContext)
    {
        LogIfError(command, interceptionContext);
    }

    public void ReaderExecuting(
        DbCommand command, DbCommandInterceptionContext<DbDataReader> interceptionContext)
    {
        LogIfNonAsync(command, interceptionContext);
    }

    public void ReaderExecuted(
        DbCommand command, DbCommandInterceptionContext<DbDataReader> interceptionContext)
    {
        LogIfError(command, interceptionContext);
    }

    public void ScalarExecuting(
        DbCommand command, DbCommandInterceptionContext<object> interceptionContext)
    {
        LogIfNonAsync(command, interceptionContext);
    }

    public void ScalarExecuted(
        DbCommand command, DbCommandInterceptionContext<object> interceptionContext)
    {
        LogIfError(command, interceptionContext);
    }

    private void LogIfNonAsync<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
        if (!interceptionContext.IsAsync)
        {
            Logger.Warn("Non-async command used: {0}", command.CommandText);
        }
    }

    private void LogIfError<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
        if (interceptionContext.Exception != null)
        {
            Logger.Error("Command {0} failed with exception {1}",
                command.CommandText, interceptionContext.Exception);
        }
    }
}
```  

<span data-ttu-id="38ac2-249">請注意，此程式碼如何使用攔截內容來探索何時以非非同步方式執行命令，並在執行命令時發生錯誤時探索。</span><span class="sxs-lookup"><span data-stu-id="38ac2-249">Notice how this code uses the interception context to discover when a command is being executed non-asynchronously and to discover when there was an error executing a command.</span></span>  
