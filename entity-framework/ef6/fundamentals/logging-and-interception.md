---
title: 記錄和攔截資料庫作業-EF6
author: divega
ms.date: 10/23/2016
ms.assetid: b5ee7eb1-88cc-456e-b53c-c67e24c3f8ca
ms.openlocfilehash: 3f06e073f3ab6e46883663620219e302d5db1d60
ms.sourcegitcommit: 2b787009fd5be5627f1189ee396e708cd130e07b
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 09/13/2018
ms.locfileid: "45490075"
---
# <a name="logging-and-intercepting-database-operations"></a><span data-ttu-id="1a25c-102">記錄和攔截資料庫作業</span><span class="sxs-lookup"><span data-stu-id="1a25c-102">Logging and intercepting database operations</span></span>
> [!NOTE]
> <span data-ttu-id="1a25c-103">**僅限 EF6 及更新版本** - Entity Framework 6 已引進此頁面中所討論的功能及 API 等等。</span><span class="sxs-lookup"><span data-stu-id="1a25c-103">**EF6 Onwards Only** - The features, APIs, etc. discussed in this page were introduced in Entity Framework 6.</span></span> <span data-ttu-id="1a25c-104">如果您使用的是較早版本，則不適用部分或全部的資訊。</span><span class="sxs-lookup"><span data-stu-id="1a25c-104">If you are using an earlier version, some or all of the information does not apply.</span></span>  

<span data-ttu-id="1a25c-105">應用程式程式碼時，可以被攔截從 Entity Framework 6，每當 Entity Framework 會將命令傳送至資料庫這個命令。</span><span class="sxs-lookup"><span data-stu-id="1a25c-105">Starting with Entity Framework 6, anytime Entity Framework sends a command to the database this command can be intercepted by application code.</span></span> <span data-ttu-id="1a25c-106">這最常用於記錄 SQL，但也可用來修改，或者中止命令。</span><span class="sxs-lookup"><span data-stu-id="1a25c-106">This is most commonly used for logging SQL, but can also be used to modify or abort the command.</span></span>  

<span data-ttu-id="1a25c-107">具體來說，EF 包含：</span><span class="sxs-lookup"><span data-stu-id="1a25c-107">Specifically, EF includes:</span></span>  

- <span data-ttu-id="1a25c-108">記錄檔的內容，類似於 LINQ to SQL 中的 DataContext.Log 內容</span><span class="sxs-lookup"><span data-stu-id="1a25c-108">A Log property for the context similar to DataContext.Log in LINQ to SQL</span></span>  
- <span data-ttu-id="1a25c-109">一種機制，以自訂的內容和格式化的輸出傳送至記錄檔</span><span class="sxs-lookup"><span data-stu-id="1a25c-109">A mechanism to customize the content and formatting of the output sent to the log</span></span>  
- <span data-ttu-id="1a25c-110">提供更高的控制彈性的攔截低階建置組塊</span><span class="sxs-lookup"><span data-stu-id="1a25c-110">Low-level building blocks for interception giving greater control/flexibility</span></span>  

## <a name="context-log-property"></a><span data-ttu-id="1a25c-111">內容記錄屬性</span><span class="sxs-lookup"><span data-stu-id="1a25c-111">Context Log property</span></span>  

<span data-ttu-id="1a25c-112">DbContext.Database.Log 屬性可以設為可接受字串的任何方法的委派。</span><span class="sxs-lookup"><span data-stu-id="1a25c-112">The DbContext.Database.Log property can be set to a delegate for any method that takes a string.</span></span> <span data-ttu-id="1a25c-113">通常它搭配任何 TextWriter 藉由將它設定為該 TextWriter 的 「 寫入 」 方法。</span><span class="sxs-lookup"><span data-stu-id="1a25c-113">Most commonly it is used with any TextWriter by setting it to the “Write” method of that TextWriter.</span></span> <span data-ttu-id="1a25c-114">目前的內容所產生的所有 SQL 將會都記錄該寫入器。</span><span class="sxs-lookup"><span data-stu-id="1a25c-114">All SQL generated by the current context will be logged to that writer.</span></span> <span data-ttu-id="1a25c-115">例如，下列程式碼會記錄至主控台的 SQL:</span><span class="sxs-lookup"><span data-stu-id="1a25c-115">For example, the following code will log SQL to the console:</span></span>  

``` csharp
using (var context = new BlogContext())
{
    context.Database.Log = Console.Write;

    // Your code here...
}
```  

<span data-ttu-id="1a25c-116">請注意該內容。Console.Write 設 Database.Log。</span><span class="sxs-lookup"><span data-stu-id="1a25c-116">Notice that context.Database.Log is set to Console.Write.</span></span> <span data-ttu-id="1a25c-117">這是所有所需的 SQL 記錄至主控台。</span><span class="sxs-lookup"><span data-stu-id="1a25c-117">This is all that is needed to log SQL to the console.</span></span>  

<span data-ttu-id="1a25c-118">讓我們加入一些簡單的查詢/插入/更新程式碼，以便我們可以看到一些輸出：</span><span class="sxs-lookup"><span data-stu-id="1a25c-118">Let’s add some simple query/insert/update code so that we can see some output:</span></span>  

``` csharp
using (var context = new BlogContext())
{
    context.Database.Log = Console.Write;

    var blog = context.Blogs.First(b => b.Title == "One Unicorn");

    blog.Posts.First().Title = "Green Eggs and Ham";

    blog.Posts.Add(new Post { Title = "I do not like them!" });

    context.SaveChangesAsync().Wait();
}
```  

<span data-ttu-id="1a25c-119">這會產生下列輸出：</span><span class="sxs-lookup"><span data-stu-id="1a25c-119">This will generate the following output:</span></span>  

``` SQL
SELECT TOP (1)
    [Extent1].[Id] AS [Id],
    [Extent1].[Title] AS [Title]
    FROM [dbo].[Blogs] AS [Extent1]
    WHERE (N'One Unicorn' = [Extent1].[Title]) AND ([Extent1].[Title] IS NOT NULL)
-- Executing at 10/8/2013 10:55:41 AM -07:00
-- Completed in 4 ms with result: SqlDataReader

SELECT
    [Extent1].[Id] AS [Id],
    [Extent1].[Title] AS [Title],
    [Extent1].[BlogId] AS [BlogId]
    FROM [dbo].[Posts] AS [Extent1]
    WHERE [Extent1].[BlogId] = @EntityKeyValue1
-- EntityKeyValue1: '1' (Type = Int32)
-- Executing at 10/8/2013 10:55:41 AM -07:00
-- Completed in 2 ms with result: SqlDataReader

UPDATE [dbo].[Posts]
SET [Title] = @0
WHERE ([Id] = @1)
-- @0: 'Green Eggs and Ham' (Type = String, Size = -1)
-- @1: '1' (Type = Int32)
-- Executing asynchronously at 10/8/2013 10:55:41 AM -07:00
-- Completed in 12 ms with result: 1

INSERT [dbo].[Posts]([Title], [BlogId])
VALUES (@0, @1)
SELECT [Id]
FROM [dbo].[Posts]
WHERE @@ROWCOUNT > 0 AND [Id] = scope_identity()
-- @0: 'I do not like them!' (Type = String, Size = -1)
-- @1: '1' (Type = Int32)
-- Executing asynchronously at 10/8/2013 10:55:41 AM -07:00
-- Completed in 2 ms with result: SqlDataReader
```  

<span data-ttu-id="1a25c-120">（請注意，這是假設已發生任何資料庫初始設定的輸出。</span><span class="sxs-lookup"><span data-stu-id="1a25c-120">(Note that this is the output assuming any database initialization has already happened.</span></span> <span data-ttu-id="1a25c-121">如果資料庫初始化時也不已經發生則會有更多的輸出顯示所有工作的移轉會在幕後，檢查或建立新的資料庫。）</span><span class="sxs-lookup"><span data-stu-id="1a25c-121">If database initialization had not already happened then there would be a lot more output showing all the work Migrations does under the covers to check for or create a new database.)</span></span>  

## <a name="what-gets-logged"></a><span data-ttu-id="1a25c-122">取得記錄的內容？</span><span class="sxs-lookup"><span data-stu-id="1a25c-122">What gets logged?</span></span>  

<span data-ttu-id="1a25c-123">將記錄記錄屬性時設定下列各項：</span><span class="sxs-lookup"><span data-stu-id="1a25c-123">When the Log property is set all of the following will be logged:</span></span>  

- <span data-ttu-id="1a25c-124">SQL 命令的所有不同的類型。</span><span class="sxs-lookup"><span data-stu-id="1a25c-124">SQL for all different kinds of commands.</span></span> <span data-ttu-id="1a25c-125">例如: </span><span class="sxs-lookup"><span data-stu-id="1a25c-125">For example:</span></span>  
    - <span data-ttu-id="1a25c-126">查詢，包括一般 LINQ 查詢、 eSQL 查詢，以及原始的查詢，例如 SqlQuery 方法</span><span class="sxs-lookup"><span data-stu-id="1a25c-126">Queries, including normal LINQ queries, eSQL queries, and raw queries from methods such as SqlQuery</span></span>  
    - <span data-ttu-id="1a25c-127">插入、 更新和刪除產生的 SaveChanges</span><span class="sxs-lookup"><span data-stu-id="1a25c-127">Inserts, updates, and deletes generated as part of SaveChanges</span></span>  
    - <span data-ttu-id="1a25c-128">正在載入查詢例如消極式載入所產生的關聯性</span><span class="sxs-lookup"><span data-stu-id="1a25c-128">Relationship loading queries such as those generated by lazy loading</span></span>  
- <span data-ttu-id="1a25c-129">參數</span><span class="sxs-lookup"><span data-stu-id="1a25c-129">Parameters</span></span>  
- <span data-ttu-id="1a25c-130">是否以非同步方式執行命令</span><span class="sxs-lookup"><span data-stu-id="1a25c-130">Whether or not the command is being executed asynchronously</span></span>  
- <span data-ttu-id="1a25c-131">時間戳記，指出啟動執行的命令</span><span class="sxs-lookup"><span data-stu-id="1a25c-131">A timestamp indicating when the command started executing</span></span>  
- <span data-ttu-id="1a25c-132">已順利完成，此命令無法藉由擲回例外狀況，或如需非同步，已取消</span><span class="sxs-lookup"><span data-stu-id="1a25c-132">Whether or not the command completed successfully, failed by throwing an exception, or, for async, was canceled</span></span>  
- <span data-ttu-id="1a25c-133">結果值的某些指示</span><span class="sxs-lookup"><span data-stu-id="1a25c-133">Some indication of the result value</span></span>  
- <span data-ttu-id="1a25c-134">執行命令所花費的大約時間量。</span><span class="sxs-lookup"><span data-stu-id="1a25c-134">The approximate amount of time it took to execute the command.</span></span> <span data-ttu-id="1a25c-135">請注意，這將命令傳送到取回結果物件的時間。</span><span class="sxs-lookup"><span data-stu-id="1a25c-135">Note that this is the time from sending the command to getting the result object back.</span></span> <span data-ttu-id="1a25c-136">它不包含讀取結果的時間。</span><span class="sxs-lookup"><span data-stu-id="1a25c-136">It does not include time to read the results.</span></span>  

<span data-ttu-id="1a25c-137">看看上面的範例輸出，每個記錄的四個命令是：</span><span class="sxs-lookup"><span data-stu-id="1a25c-137">Looking at the example output above, each of the four commands logged are:</span></span>  

- <span data-ttu-id="1a25c-138">查詢內容呼叫所產生。Blogs.First</span><span class="sxs-lookup"><span data-stu-id="1a25c-138">The query resulting from the call to context.Blogs.First</span></span>  
    - <span data-ttu-id="1a25c-139">請注意，因為此查詢會無法運作的 ToString 方法取得 SQL"First"不會提供無法在其呼叫 ToString 的 IQueryable</span><span class="sxs-lookup"><span data-stu-id="1a25c-139">Notice that the ToString method of getting the SQL would not have worked for this query since “First” does not provide an IQueryable on which ToString could be called</span></span>  
- <span data-ttu-id="1a25c-140">查詢所產生的消極式載入的部落格。貼文</span><span class="sxs-lookup"><span data-stu-id="1a25c-140">The query resulting from the lazy-loading of blog.Posts</span></span>  
    - <span data-ttu-id="1a25c-141">請注意，在發生的消極式載入的金鑰值的參數詳細資料</span><span class="sxs-lookup"><span data-stu-id="1a25c-141">Notice the parameter details for the key value for which lazy loading is happening</span></span>  
    - <span data-ttu-id="1a25c-142">只有設定為非預設值的屬性參數的記錄。</span><span class="sxs-lookup"><span data-stu-id="1a25c-142">Only properties of the parameter that are set to non-default values are logged.</span></span> <span data-ttu-id="1a25c-143">例如，[大小] 屬性只會顯示它是否不是零。</span><span class="sxs-lookup"><span data-stu-id="1a25c-143">For example, the Size property is only shown if it is non-zero.</span></span>  
- <span data-ttu-id="1a25c-144">兩個命令所產生的 SaveChangesAsync;另一個用於更新，以變更文章標題，另一個則用於在 [插入] 來新增新的貼文</span><span class="sxs-lookup"><span data-stu-id="1a25c-144">Two commands resulting from SaveChangesAsync; one for the update to change a post title, the other for an insert to add a new post</span></span>  
    - <span data-ttu-id="1a25c-145">請注意的 FK 和 Title 屬性的參數詳細資料</span><span class="sxs-lookup"><span data-stu-id="1a25c-145">Notice the parameter details for the FK and Title properties</span></span>  
    - <span data-ttu-id="1a25c-146">請注意這些命令會以非同步方式執行</span><span class="sxs-lookup"><span data-stu-id="1a25c-146">Notice that these commands are being executed asynchronously</span></span>  

## <a name="logging-to-different-places"></a><span data-ttu-id="1a25c-147">記錄到不同的地方</span><span class="sxs-lookup"><span data-stu-id="1a25c-147">Logging to different places</span></span>  

<span data-ttu-id="1a25c-148">記錄至如上所示的主控台是超級簡單。</span><span class="sxs-lookup"><span data-stu-id="1a25c-148">As shown above logging to the console is super easy.</span></span> <span data-ttu-id="1a25c-149">它也很容易使用各種記錄記憶體、 檔案等等的[TextWriter](https://msdn.microsoft.com/library/system.io.textwriter.aspx)。</span><span class="sxs-lookup"><span data-stu-id="1a25c-149">It’s also easy to log to memory, file, etc. by using different kinds of [TextWriter](https://msdn.microsoft.com/library/system.io.textwriter.aspx).</span></span>  

<span data-ttu-id="1a25c-150">如果您已熟悉 SQL，您可能會發現，在 LINQ to SQL 記錄檔的內容設定為實際 TextWriter 物件 (例如 Console.Out) 時記錄設定為接受字串 （例如方法的 EF 中的 LINQConsole.Write 或 Console.Out.Write)。</span><span class="sxs-lookup"><span data-stu-id="1a25c-150">If you are familiar with LINQ to SQL you might notice that in LINQ to SQL the Log property is set to the actual TextWriter object (for example, Console.Out) while in EF the Log property is set to a method that accepts a string (for example, Console.Write or Console.Out.Write).</span></span> <span data-ttu-id="1a25c-151">這是讓 EF 從 TextWriter 分離接受任何委派，可做為接收器的字串。</span><span class="sxs-lookup"><span data-stu-id="1a25c-151">The reason for this is to decouple EF from TextWriter by accepting any delegate that can act as a sink for strings.</span></span> <span data-ttu-id="1a25c-152">例如，假設您已經有一些記錄架構，它會定義記錄方法就像這樣：</span><span class="sxs-lookup"><span data-stu-id="1a25c-152">For example, imagine that you already have some logging framework and it defines a logging method like so:</span></span>  

``` csharp
public class MyLogger
{
    public void Log(string component, string message)
    {
        Console.WriteLine("Component: {0} Message: {1} ", component, message);
    }
}
```  

<span data-ttu-id="1a25c-153">這無法連接到 EF 記錄檔的內容如下：</span><span class="sxs-lookup"><span data-stu-id="1a25c-153">This could be hooked up to the EF Log property like this:</span></span>  

``` csharp
var logger = new MyLogger();
context.Database.Log = s => logger.Log("EFApp", s);
```  

## <a name="result-logging"></a><span data-ttu-id="1a25c-154">結果記錄</span><span class="sxs-lookup"><span data-stu-id="1a25c-154">Result logging</span></span>  

<span data-ttu-id="1a25c-155">預設記錄器記錄命令文字 (SQL)、 參數和 「 執行 」 列加上時間戳記之前的命令傳送至資料庫。</span><span class="sxs-lookup"><span data-stu-id="1a25c-155">The default logger logs command text (SQL), parameters, and the “Executing” line with a timestamp before the command is sent to the database.</span></span> <span data-ttu-id="1a25c-156">「 已完成 」 包含已耗用時間的資料行是記錄的執行下列命令。</span><span class="sxs-lookup"><span data-stu-id="1a25c-156">A “completed” line containing elapsed time is logged following execution of the command.</span></span>  

<span data-ttu-id="1a25c-157">請注意，如需非同步命令的 「 已完成 」 的一行時，不會記錄直到非同步工作實際完成、 失敗，或已取消。</span><span class="sxs-lookup"><span data-stu-id="1a25c-157">Note that for async commands the “completed” line is not logged until the async task actually completes, fails, or is canceled.</span></span>  

<span data-ttu-id="1a25c-158">「 已完成 」 列包含不同的資訊，根據命令和已成功執行的類型。</span><span class="sxs-lookup"><span data-stu-id="1a25c-158">The “completed” line contains different information depending on the type of command and whether or not execution was successful.</span></span>  

### <a name="successful-execution"></a><span data-ttu-id="1a25c-159">成功執行</span><span class="sxs-lookup"><span data-stu-id="1a25c-159">Successful execution</span></span>  

<span data-ttu-id="1a25c-160">若是成功完成輸出的命令是 「 x ms 結果中的已完成: 」 後面的結果為何的某些指示。</span><span class="sxs-lookup"><span data-stu-id="1a25c-160">For commands that complete successfully the output is “Completed in x ms with result: “ followed by some indication of what the result was.</span></span> <span data-ttu-id="1a25c-161">傳回結果的資料讀取器的命令的指示是種[DbDataReader](https://msdn.microsoft.com/library/system.data.common.dbdatareader.aspx)傳回。</span><span class="sxs-lookup"><span data-stu-id="1a25c-161">For commands that return a data reader the result indication is the type of [DbDataReader](https://msdn.microsoft.com/library/system.data.common.dbdatareader.aspx) returned.</span></span> <span data-ttu-id="1a25c-162">傳回整數值，例如更新的命令如上所示的結果所示的命令會為整數。</span><span class="sxs-lookup"><span data-stu-id="1a25c-162">For commands that return an integer value such as the update command shown above the result shown is that integer.</span></span>  

### <a name="failed-execution"></a><span data-ttu-id="1a25c-163">執行失敗</span><span class="sxs-lookup"><span data-stu-id="1a25c-163">Failed execution</span></span>  

<span data-ttu-id="1a25c-164">如需藉由擲回例外狀況失敗的命令，輸出會包含例外狀況的訊息。</span><span class="sxs-lookup"><span data-stu-id="1a25c-164">For commands that fail by throwing an exception, the output contains the message from the exception.</span></span> <span data-ttu-id="1a25c-165">比方說，使用 SqlQuery 來查詢存在的資料表會記錄檔中的結果輸出類似如下：</span><span class="sxs-lookup"><span data-stu-id="1a25c-165">For example, using SqlQuery to query against a table that does exist will result in log output something like this:</span></span>  

``` SQL
SELECT * from ThisTableIsMissing
-- Executing at 5/13/2013 10:19:05 AM
-- Failed in 1 ms with error: Invalid object name 'ThisTableIsMissing'.
```  

### <a name="canceled-execution"></a><span data-ttu-id="1a25c-166">已取消的執行</span><span class="sxs-lookup"><span data-stu-id="1a25c-166">Canceled execution</span></span>  

<span data-ttu-id="1a25c-167">非同步命令取消工作時，結果可能會失敗並發生例外狀況，因為這是什麼基礎 ADO.NET 提供者通常會嘗試取消時。</span><span class="sxs-lookup"><span data-stu-id="1a25c-167">For async commands where the task is canceled the result could be failure with an exception, since this is what the underlying ADO.NET provider often does when an attempt is made to cancel.</span></span> <span data-ttu-id="1a25c-168">如果這不會發生，且工作已完全取消，則輸出會看起來像這樣：</span><span class="sxs-lookup"><span data-stu-id="1a25c-168">If this doesn’t happen and the task is canceled cleanly then the output will look something like this:</span></span>  

```  
update Blogs set Title = 'No' where Id = -1
-- Executing asynchronously at 5/13/2013 10:21:10 AM
-- Canceled in 1 ms
```  

## <a name="changing-log-content-and-formatting"></a><span data-ttu-id="1a25c-169">變更記錄檔的內容和格式</span><span class="sxs-lookup"><span data-stu-id="1a25c-169">Changing log content and formatting</span></span>  

<span data-ttu-id="1a25c-170">在幕後屬性會使 Database.Log DatabaseLogFormatter 物件使用。</span><span class="sxs-lookup"><span data-stu-id="1a25c-170">Under the covers the Database.Log property makes use of a DatabaseLogFormatter object.</span></span> <span data-ttu-id="1a25c-171">這個物件有效地繫結的委派，接受字串和 DbContext IDbCommandInterceptor 實作 （如下所示）。</span><span class="sxs-lookup"><span data-stu-id="1a25c-171">This object effectively binds an IDbCommandInterceptor implementation (see below) to a delegate that accepts strings and a DbContext.</span></span> <span data-ttu-id="1a25c-172">這表示，在 DatabaseLogFormatter 會呼叫方法之前和之後執行命令的 EF。</span><span class="sxs-lookup"><span data-stu-id="1a25c-172">This means that methods on DatabaseLogFormatter are called before and after the execution of commands by EF.</span></span> <span data-ttu-id="1a25c-173">這些 DatabaseLogFormatter 方法收集和記錄檔輸出格式化並將它傳送給委派。</span><span class="sxs-lookup"><span data-stu-id="1a25c-173">These DatabaseLogFormatter methods gather and format log output and send it to the delegate.</span></span>  

### <a name="customizing-databaselogformatter"></a><span data-ttu-id="1a25c-174">自訂 DatabaseLogFormatter</span><span class="sxs-lookup"><span data-stu-id="1a25c-174">Customizing DatabaseLogFormatter</span></span>  

<span data-ttu-id="1a25c-175">變更記錄的內容和格式的方式，即可建立新的類別衍生自 DatabaseLogFormatter 並覆寫適當的方法。</span><span class="sxs-lookup"><span data-stu-id="1a25c-175">Changing what is logged and how it is formatted can be achieved by creating a new class that derives from DatabaseLogFormatter and overrides methods as appropriate.</span></span> <span data-ttu-id="1a25c-176">若要覆寫最常見的方法如下：</span><span class="sxs-lookup"><span data-stu-id="1a25c-176">The most common methods to override are:</span></span>  

- <span data-ttu-id="1a25c-177">LogCommand-覆寫此變更會在執行之前的記錄命令的方式。</span><span class="sxs-lookup"><span data-stu-id="1a25c-177">LogCommand – Override this to change how commands are logged before they are executed.</span></span> <span data-ttu-id="1a25c-178">根據預設 LogCommand 呼叫 LogParameter 針對每個參數;您可以選擇覆寫中相同，或改為以不同方式處理參數。</span><span class="sxs-lookup"><span data-stu-id="1a25c-178">By default LogCommand calls LogParameter for each parameter; you may choose to do the same in your override or handle parameters differently instead.</span></span>  
- <span data-ttu-id="1a25c-179">LogResult-覆寫這個值來變更執行命令的結果記錄的方式。</span><span class="sxs-lookup"><span data-stu-id="1a25c-179">LogResult – Override this to change how the outcome from executing a command is logged.</span></span>  
- <span data-ttu-id="1a25c-180">LogParameter-覆寫此選項以變更與參數記錄的內容。</span><span class="sxs-lookup"><span data-stu-id="1a25c-180">LogParameter – Override this to change the formatting and content of parameter logging.</span></span>  

<span data-ttu-id="1a25c-181">例如，假設我們想要記錄只有一行，每個命令傳送至資料庫之前。</span><span class="sxs-lookup"><span data-stu-id="1a25c-181">For example, suppose we wanted to log just a single line before each command is sent to the database.</span></span> <span data-ttu-id="1a25c-182">這可以透過兩個覆寫來完成：</span><span class="sxs-lookup"><span data-stu-id="1a25c-182">This can be done with two overrides:</span></span>  

- <span data-ttu-id="1a25c-183">覆寫要格式化和寫入 SQL 單行 LogCommand</span><span class="sxs-lookup"><span data-stu-id="1a25c-183">Override LogCommand to format and write the single line of SQL</span></span>  
- <span data-ttu-id="1a25c-184">覆寫 LogResult 不執行任何動作。</span><span class="sxs-lookup"><span data-stu-id="1a25c-184">Override LogResult to do nothing.</span></span>  

<span data-ttu-id="1a25c-185">程式碼看起來像這樣：</span><span class="sxs-lookup"><span data-stu-id="1a25c-185">The code would look something like this:</span></span>

``` csharp
public class OneLineFormatter : DatabaseLogFormatter
{
    public OneLineFormatter(DbContext context, Action<string> writeAction)
        : base(context, writeAction)
    {
    }

    public override void LogCommand<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
        Write(string.Format(
            "Context '{0}' is executing command '{1}'{2}",
            Context.GetType().Name,
            command.CommandText.Replace(Environment.NewLine, ""),
            Environment.NewLine));
    }

    public override void LogResult<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
    }
}
```  

<span data-ttu-id="1a25c-186">若要記錄輸出只呼叫 Write 方法會將輸出傳送給已設定的寫入委派。</span><span class="sxs-lookup"><span data-stu-id="1a25c-186">To log output simply call the Write method which will send output to the configured write delegate.</span></span>  

<span data-ttu-id="1a25c-187">（請注意，此程式碼會簡化移除分行符號只是做為範例。</span><span class="sxs-lookup"><span data-stu-id="1a25c-187">(Note that this code does simplistic removal of line breaks just as an example.</span></span> <span data-ttu-id="1a25c-188">它可能不適用於檢視複雜的 SQL。）</span><span class="sxs-lookup"><span data-stu-id="1a25c-188">It will likely not work well for viewing complex SQL.)</span></span>  

### <a name="setting-the-databaselogformatter"></a><span data-ttu-id="1a25c-189">設定 DatabaseLogFormatter</span><span class="sxs-lookup"><span data-stu-id="1a25c-189">Setting the DatabaseLogFormatter</span></span>  

<span data-ttu-id="1a25c-190">新的 DatabaseLogFormatter 類別建立之後就必須使用 EF 註冊。</span><span class="sxs-lookup"><span data-stu-id="1a25c-190">Once a new DatabaseLogFormatter class has been created it needs to be registered with EF.</span></span> <span data-ttu-id="1a25c-191">這是使用程式碼為基礎的組態。</span><span class="sxs-lookup"><span data-stu-id="1a25c-191">This is done using code-based configuration.</span></span> <span data-ttu-id="1a25c-192">簡單的說這表示建立新的類別衍生自 DbConfiguration 中，在您的 DbContext 類別相同的組件，並在這個新類別的建構函式呼叫 SetDatabaseLogFormatter。</span><span class="sxs-lookup"><span data-stu-id="1a25c-192">In a nutshell this means creating a new class that derives from DbConfiguration in the same assembly as your DbContext class and then calling SetDatabaseLogFormatter in the constructor of this new class.</span></span> <span data-ttu-id="1a25c-193">例如: </span><span class="sxs-lookup"><span data-stu-id="1a25c-193">For example:</span></span>  

``` csharp
public class MyDbConfiguration : DbConfiguration
{
    public MyDbConfiguration()
    {
        SetDatabaseLogFormatter(
            (context, writeAction) => new OneLineFormatter(context, writeAction));
    }
}
```  

### <a name="using-the-new-databaselogformatter"></a><span data-ttu-id="1a25c-194">使用新的 DatabaseLogFormatter</span><span class="sxs-lookup"><span data-stu-id="1a25c-194">Using the new DatabaseLogFormatter</span></span>  

<span data-ttu-id="1a25c-195">每當設定 Database.Log，現在將會使用這個新 DatabaseLogFormatter。</span><span class="sxs-lookup"><span data-stu-id="1a25c-195">This new DatabaseLogFormatter will now be used anytime Database.Log is set.</span></span> <span data-ttu-id="1a25c-196">因此，第 1 部分中執行程式碼會立即導致下列輸出：</span><span class="sxs-lookup"><span data-stu-id="1a25c-196">So, running the code from part 1 will now result in the following output:</span></span>  

```  
Context 'BlogContext' is executing command 'SELECT TOP (1) [Extent1].[Id] AS [Id], [Extent1].[Title] AS [Title]FROM [dbo].[Blogs] AS [Extent1]WHERE (N'One Unicorn' = [Extent1].[Title]) AND ([Extent1].[Title] IS NOT NULL)'
Context 'BlogContext' is executing command 'SELECT [Extent1].[Id] AS [Id], [Extent1].[Title] AS [Title], [Extent1].[BlogId] AS [BlogId]FROM [dbo].[Posts] AS [Extent1]WHERE [Extent1].[BlogId] = @EntityKeyValue1'
Context 'BlogContext' is executing command 'update [dbo].[Posts]set [Title] = @0where ([Id] = @1)'
Context 'BlogContext' is executing command 'insert [dbo].[Posts]([Title], [BlogId])values (@0, @1)select [Id]from [dbo].[Posts]where @@rowcount > 0 and [Id] = scope_identity()'
```  

## <a name="interception-building-blocks"></a><span data-ttu-id="1a25c-197">攔截建置組塊</span><span class="sxs-lookup"><span data-stu-id="1a25c-197">Interception building blocks</span></span>  

<span data-ttu-id="1a25c-198">到目前為止我們已探討如何使用 DbContext.Database.Log 記錄 EF 所產生的 SQL。</span><span class="sxs-lookup"><span data-stu-id="1a25c-198">So far we have looked at how to use DbContext.Database.Log to log the SQL generated by EF.</span></span> <span data-ttu-id="1a25c-199">但這段程式碼透過更一般的攔截的一些低階建置組塊是實際的相當精簡的外觀。</span><span class="sxs-lookup"><span data-stu-id="1a25c-199">But this code is actually a relatively thin façade over some low-level building blocks for more general interception.</span></span>  

### <a name="interception-interfaces"></a><span data-ttu-id="1a25c-200">攔截介面</span><span class="sxs-lookup"><span data-stu-id="1a25c-200">Interception interfaces</span></span>  

<span data-ttu-id="1a25c-201">攔截程式碼會攔截介面的概念而建置。</span><span class="sxs-lookup"><span data-stu-id="1a25c-201">The interception code is built around the concept of interception interfaces.</span></span> <span data-ttu-id="1a25c-202">這些介面繼承自 IDbInterceptor，並定義當 EF 執行某個動作時所呼叫的方法。</span><span class="sxs-lookup"><span data-stu-id="1a25c-202">These interfaces inherit from IDbInterceptor and define methods that are called when EF performs some action.</span></span> <span data-ttu-id="1a25c-203">目的是物件的要有一個介面，每個類型被攔截。</span><span class="sxs-lookup"><span data-stu-id="1a25c-203">The intent is to have one interface per type of object being intercepted.</span></span> <span data-ttu-id="1a25c-204">比方說，IDbCommandInterceptor 介面定義之前 EF 會呼叫 ExecuteNonQuery、 ExecuteScalar、 ExecuteReader 和相關的方法，呼叫的方法。</span><span class="sxs-lookup"><span data-stu-id="1a25c-204">For example, the IDbCommandInterceptor interface defines methods that are called before EF makes a call to ExecuteNonQuery, ExecuteScalar, ExecuteReader, and related methods.</span></span> <span data-ttu-id="1a25c-205">同樣地，介面會定義每個作業完成時呼叫的方法。</span><span class="sxs-lookup"><span data-stu-id="1a25c-205">Likewise, the interface defines methods that are called when each of these operations completes.</span></span> <span data-ttu-id="1a25c-206">我們探討了上述 DatabaseLogFormatter 類別會實作這個介面來記錄的命令。</span><span class="sxs-lookup"><span data-stu-id="1a25c-206">The DatabaseLogFormatter class that we looked at above implements this interface to log commands.</span></span>  

### <a name="the-interception-context"></a><span data-ttu-id="1a25c-207">攔截內容</span><span class="sxs-lookup"><span data-stu-id="1a25c-207">The interception context</span></span>  

<span data-ttu-id="1a25c-208">它查看任何攔截器介面上定義的方法是很明顯，每次呼叫指定的型別的物件 DbInterceptionContext 或某種類型衍生自這例如 DbCommandInterceptionContext\<\>。</span><span class="sxs-lookup"><span data-stu-id="1a25c-208">Looking at the methods defined on any of the interceptor interfaces it is apparent that every call is given an object of type DbInterceptionContext or some type derived from this such as DbCommandInterceptionContext\<\>.</span></span> <span data-ttu-id="1a25c-209">此物件包含內容資訊所花的 EF 的動作。</span><span class="sxs-lookup"><span data-stu-id="1a25c-209">This object contains contextual information about the action that EF is taking.</span></span> <span data-ttu-id="1a25c-210">例如，如果此動作會 DbContext 代表正在執行，DbContext 包含在 DbInterceptionContext。</span><span class="sxs-lookup"><span data-stu-id="1a25c-210">For example, if the action is being taken on behalf of a DbContext, then the DbContext is included in the DbInterceptionContext.</span></span> <span data-ttu-id="1a25c-211">同樣地，對於以非同步方式執行的命令，IsAsync 旗標上設定 DbCommandInterceptionContext。</span><span class="sxs-lookup"><span data-stu-id="1a25c-211">Similarly, for commands that are being executed asynchronously, the IsAsync flag is set on DbCommandInterceptionContext.</span></span>  

### <a name="result-handling"></a><span data-ttu-id="1a25c-212">結果處理</span><span class="sxs-lookup"><span data-stu-id="1a25c-212">Result handling</span></span>  

<span data-ttu-id="1a25c-213">DbCommandInterceptionContext\< \>類別包含名為結果、 OriginalResult、 例外狀況，以及 OriginalException 的屬性。</span><span class="sxs-lookup"><span data-stu-id="1a25c-213">The DbCommandInterceptionContext\<\> class contains a properties called Result, OriginalResult, Exception, and OriginalException.</span></span> <span data-ttu-id="1a25c-214">這些屬性設定為 null/零對執行作業之前稱為攔截方法的呼叫 — 也就是為...執行方法。</span><span class="sxs-lookup"><span data-stu-id="1a25c-214">These properties are set to null/zero for calls to the interception methods that are called before the operation is executed — that is, for the …Executing methods.</span></span> <span data-ttu-id="1a25c-215">如果作業執行，且成功，然後結果和 OriginalResult 會設定為作業的結果。</span><span class="sxs-lookup"><span data-stu-id="1a25c-215">If the operation is executed and succeeds, then Result and OriginalResult are set to the result of the operation.</span></span> <span data-ttu-id="1a25c-216">這些值接著會出現在執行此作業之後稱為攔截方法 — 也就是上...執行的方法。</span><span class="sxs-lookup"><span data-stu-id="1a25c-216">These values can then be observed in the interception methods that are called after the operation has executed — that is, on the …Executed methods.</span></span> <span data-ttu-id="1a25c-217">同樣地，如果作業會擲回，然後 例外 和 OriginalException 屬性會設定。</span><span class="sxs-lookup"><span data-stu-id="1a25c-217">Likewise, if the operation throws, then the Exception and OriginalException properties will be set.</span></span>  

#### <a name="suppressing-execution"></a><span data-ttu-id="1a25c-218">隱藏執行</span><span class="sxs-lookup"><span data-stu-id="1a25c-218">Suppressing execution</span></span>  

<span data-ttu-id="1a25c-219">如果攔截器設定的 Result 屬性之前已執行的命令 (其中一種...執行方法） 然後 EF 實際執行的命令，將不會嘗試，但會改為只使用結果集。</span><span class="sxs-lookup"><span data-stu-id="1a25c-219">If an interceptor sets the Result property before the command has executed (in one of the …Executing methods) then EF will not attempt to actually execute the command, but will instead just use the result set.</span></span> <span data-ttu-id="1a25c-220">換句話說，攔截器可以隱藏執行命令，但有 EF 繼續執行命令。</span><span class="sxs-lookup"><span data-stu-id="1a25c-220">In other words, the interceptor can suppress execution of the command but have EF continue as if the command had been executed.</span></span>  

<span data-ttu-id="1a25c-221">這可能用法的範例是命令批次，過去已完成，但包裝提供者。</span><span class="sxs-lookup"><span data-stu-id="1a25c-221">An example of how this might be used is the command batching that has traditionally been done with a wrapping provider.</span></span> <span data-ttu-id="1a25c-222">攔截器會儲存供稍後執行命令，以批次，但會 「 假裝 」 ef 命令有正常執行。</span><span class="sxs-lookup"><span data-stu-id="1a25c-222">The interceptor would store the command for later execution as a batch but would “pretend” to EF that the command had executed as normal.</span></span> <span data-ttu-id="1a25c-223">請注意，它需要比這更實作批次處理，但這是如何變更攔截結果可能會使用的範例。</span><span class="sxs-lookup"><span data-stu-id="1a25c-223">Note that it requires more than this to implement batching, but this is an example of how changing the interception result might be used.</span></span>  

<span data-ttu-id="1a25c-224">也可以隱藏執行中的其中一個設定的例外狀況屬性...執行方法。</span><span class="sxs-lookup"><span data-stu-id="1a25c-224">Execution can also be suppressed by setting the Exception property in one of the …Executing methods.</span></span> <span data-ttu-id="1a25c-225">這會導致 EF 繼續執行作業失敗所擲回指定的例外狀況。</span><span class="sxs-lookup"><span data-stu-id="1a25c-225">This causes EF to continue as if execution of the operation had failed by throwing the given exception.</span></span> <span data-ttu-id="1a25c-226">這當然，可能會導致應用程式當機，但也可能是暫時性例外狀況或其他一些 EF 所處理的例外狀況。</span><span class="sxs-lookup"><span data-stu-id="1a25c-226">This may, of course, cause the application to crash, but it may also be a transient exception or some other exception that is handled by EF.</span></span> <span data-ttu-id="1a25c-227">比方說，這可用在測試環境中測試應用程式的行為，命令執行失敗時。</span><span class="sxs-lookup"><span data-stu-id="1a25c-227">For example, this could be used in test environments to test the behavior of an application when command execution fails.</span></span>  

#### <a name="changing-the-result-after-execution"></a><span data-ttu-id="1a25c-228">在執行後變更結果</span><span class="sxs-lookup"><span data-stu-id="1a25c-228">Changing the result after execution</span></span>  

<span data-ttu-id="1a25c-229">如果攔截器設定的 Result 屬性命令執行之後 (其中一種...執行方法） 則 EF 會使用已變更的結果，而不是實際上從作業傳回的結果。</span><span class="sxs-lookup"><span data-stu-id="1a25c-229">If an interceptor sets the Result property after the command has executed (in one of the …Executed methods) then EF will use the changed result instead of the result that was actually returned from the operation.</span></span> <span data-ttu-id="1a25c-230">同樣地，如果命令執行之後，攔截器會將例外狀況屬性，然後 EF 會擲回組例外狀況，如果作業已擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="1a25c-230">Similarly, if an interceptor sets the Exception property after the command has executed, then EF will throw the set exception as if the operation had thrown the exception.</span></span>  

<span data-ttu-id="1a25c-231">攔截器也可以設定為 null 表示應該擲回任何例外狀況的例外狀況屬性。</span><span class="sxs-lookup"><span data-stu-id="1a25c-231">An interceptor can also set the Exception property to null to indicate that no exception should be thrown.</span></span> <span data-ttu-id="1a25c-232">如果執行的作業失敗，但攔截器希望繼續像作業已成功的 EF，這非常有用。</span><span class="sxs-lookup"><span data-stu-id="1a25c-232">This can be useful if execution of the operation failed but the interceptor wishes EF to continue as if the operation had succeeded.</span></span> <span data-ttu-id="1a25c-233">這通常也牽涉到設定的結果，好讓 EF 必須持續使用一些結果值。</span><span class="sxs-lookup"><span data-stu-id="1a25c-233">This usually also involves setting the Result so that EF has some result value to work with as it continues.</span></span>  

#### <a name="originalresult-and-originalexception"></a><span data-ttu-id="1a25c-234">OriginalResult 和 OriginalException</span><span class="sxs-lookup"><span data-stu-id="1a25c-234">OriginalResult and OriginalException</span></span>  

<span data-ttu-id="1a25c-235">EF 執行作業之後它會設定 結果 和 OriginalResult 屬性如果執行未失敗或例外狀況和 OriginalException 屬性，如果執行失敗，發生例外狀況。</span><span class="sxs-lookup"><span data-stu-id="1a25c-235">After EF has executed an operation it will set either the Result and OriginalResult properties if execution did not fail or the Exception and OriginalException properties if execution failed with an exception.</span></span>  

<span data-ttu-id="1a25c-236">OriginalResult 和 OriginalException 屬性是唯讀，並在實際執行作業之後才設定 EF 所。</span><span class="sxs-lookup"><span data-stu-id="1a25c-236">The OriginalResult and OriginalException properties are read-only and are only set by EF after actually executing an operation.</span></span> <span data-ttu-id="1a25c-237">無法設定這些屬性，由攔截器。</span><span class="sxs-lookup"><span data-stu-id="1a25c-237">These properties cannot be set by interceptors.</span></span> <span data-ttu-id="1a25c-238">這表示任何攔截器可以區別例外狀況或已經設定了某些其他攔截器而不是實際的例外狀況的結果或執行作業時所發生的結果。</span><span class="sxs-lookup"><span data-stu-id="1a25c-238">This means that any interceptor can distinguish between an exception or result that has been set by some other interceptor as opposed to the real exception or result that occurred when the operation was executed.</span></span>  

### <a name="registering-interceptors"></a><span data-ttu-id="1a25c-239">註冊攔截器</span><span class="sxs-lookup"><span data-stu-id="1a25c-239">Registering interceptors</span></span>  

<span data-ttu-id="1a25c-240">一旦建立實作一或多個攔截介面的類別，它可以向 EF 使用 DbInterception 類別。</span><span class="sxs-lookup"><span data-stu-id="1a25c-240">Once a class that implements one or more of the interception interfaces has been created it can be registered with EF using the DbInterception class.</span></span> <span data-ttu-id="1a25c-241">例如: </span><span class="sxs-lookup"><span data-stu-id="1a25c-241">For example:</span></span>  

``` csharp
DbInterception.Add(new NLogCommandInterceptor());
```  

<span data-ttu-id="1a25c-242">攔截器也可以註冊在應用程式定義域層級使用 DbConfiguration 程式碼為基礎的組態機制。</span><span class="sxs-lookup"><span data-stu-id="1a25c-242">Interceptors can also be registered at the app-domain level using the DbConfiguration code-based configuration mechanism.</span></span>  

### <a name="example-logging-to-nlog"></a><span data-ttu-id="1a25c-243">範例： 記錄 NLog</span><span class="sxs-lookup"><span data-stu-id="1a25c-243">Example: Logging to NLog</span></span>  

<span data-ttu-id="1a25c-244">讓我們將所有此一起範例的方法使用 IDbCommandInterceptor 並[NLog](http://nlog-project.org/)來：</span><span class="sxs-lookup"><span data-stu-id="1a25c-244">Let’s put all this together into an example that using IDbCommandInterceptor and [NLog](http://nlog-project.org/) to:</span></span>  

- <span data-ttu-id="1a25c-245">記錄非以非同步的方式執行的任何命令的警告</span><span class="sxs-lookup"><span data-stu-id="1a25c-245">Log a warning for any command that is executed non-asynchronously</span></span>  
- <span data-ttu-id="1a25c-246">記錄檔執行時，會擲回的任何命令的錯誤</span><span class="sxs-lookup"><span data-stu-id="1a25c-246">Log an error for any command that throws when executed</span></span>  

<span data-ttu-id="1a25c-247">以下是類別的程式碼的記錄，它應該註冊，如上所示：</span><span class="sxs-lookup"><span data-stu-id="1a25c-247">Here’s the class that does the logging, which should be registered as shown above:</span></span>  

``` csharp
public class NLogCommandInterceptor : IDbCommandInterceptor
{
    private static readonly Logger Logger = LogManager.GetCurrentClassLogger();

    public void NonQueryExecuting(
        DbCommand command, DbCommandInterceptionContext<int> interceptionContext)
    {
        LogIfNonAsync(command, interceptionContext);
    }

    public void NonQueryExecuted(
        DbCommand command, DbCommandInterceptionContext<int> interceptionContext)
    {
        LogIfError(command, interceptionContext);
    }

    public void ReaderExecuting(
        DbCommand command, DbCommandInterceptionContext<DbDataReader> interceptionContext)
    {
        LogIfNonAsync(command, interceptionContext);
    }

    public void ReaderExecuted(
        DbCommand command, DbCommandInterceptionContext<DbDataReader> interceptionContext)
    {
        LogIfError(command, interceptionContext);
    }

    public void ScalarExecuting(
        DbCommand command, DbCommandInterceptionContext<object> interceptionContext)
    {
        LogIfNonAsync(command, interceptionContext);
    }

    public void ScalarExecuted(
        DbCommand command, DbCommandInterceptionContext<object> interceptionContext)
    {
        LogIfError(command, interceptionContext);
    }

    private void LogIfNonAsync<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
        if (!interceptionContext.IsAsync)
        {
            Logger.Warn("Non-async command used: {0}", command.CommandText);
        }
    }

    private void LogIfError<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
        if (interceptionContext.Exception != null)
        {
            Logger.Error("Command {0} failed with exception {1}",
                command.CommandText, interceptionContext.Exception);
        }
    }
}
```  

<span data-ttu-id="1a25c-248">請注意，此程式碼如何使用攔截內容，來探索非以非同步方式執行命令時，以及探索時執行命令時發生錯誤。</span><span class="sxs-lookup"><span data-stu-id="1a25c-248">Notice how this code uses the interception context to discover when a command is being executed non-asynchronously and to discover when there was an error executing a command.</span></span>  
